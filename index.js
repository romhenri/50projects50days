var ar = ["R", "O", "M"];

var times = 8;

function addPositions(timesToDo) {
	let times = timesToDo;

	for (t = 0; t <= times; t++) {
		console.log("Ativado essa vez.");
	}

	return times;
}

// for (t = 1; t <= times; t++) {
// 	console.log(`Ativado essa vez ${t}.`);
// }

// console.log(addPositions(3));

// for (t = times; (t = 0); t--) {
// 	console.log("Ativado essa vez.");
// }
